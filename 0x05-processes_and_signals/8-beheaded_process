#!/usr/bin/env bash
# This script forcefully terminates the '7-highlander' process using the SIGKILL signal (9).

# Use the pkill command to send SIGKILL (signal 9).
# -9: Specifies the signal to be sent (SIGKILL).
# -f: Matches the pattern against the full command line.
pkill -9 -f "7-highlander"

# Alternatively, using kill and pgrep:
# PID=$(pgrep -f "7-highlander")
# if [ -n "$PID" ]; then
#     kill -9 "$PID"
# fi
